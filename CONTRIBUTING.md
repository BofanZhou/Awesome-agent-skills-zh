# 贡献指南

> 想直接复制就填？去 **[TEMPLATE.md](./TEMPLATE.md)** 拿现成模板（场景 A / B / C + 自检清单）。

## 收录标准
1. 必须是 **Agent Skill**：有 `SKILL.md`，能被 Claude Code / Codex / pi 等加载。
2. 有文档：README 或 SKILL.md 内说明用途与触发条件。
3. 有维护迹象：近期有提交、issue 有回应，或 stars 较高。

## 提交流程
1. 在 `README.md` 找到合适的分类小节。按用途按簇分类。
2. 按格式追加：`- **技能名** — 一句中文用途说明（保留英文术语）`
3. 如为新来源仓库，在仓库对应小节补充：**来源仓库**链接 + 一句话介绍。
4. 发起 Pull Request，说明收录理由（stars / 实际使用 / 文档质量）。

### 三种场景速览（详见 TEMPLATE.md）
- **场景 A**：给已收录仓库加一个技能 → 只加一行，不动其他。
- **场景 B**：新增一个来源仓库簇 → 在现有 9 大分类内追加一个 `### 仓库` 块。
- **场景 C**：新增一个 9 大分类之外的大分类 → 新建 `##` 块 + 目录加条目 + 更新徽章统计。
  - 新增分类记得在 `##` 标题上方放 `<a id="<英文 id>"></a>` 锚点，并保留分类末尾的 `[⬆ 返回目录](#toc)`。

## 格式规范
- 中文描述 ≤ 60 字，开头动词（生成 / 审查 / 构建 / 检索…）。
- 库名、API 名、平台名保留英文。
- 不添加表情符号与无信息量的营销话术。

## 维护约定
- 每条描述只描述「它能做什么」，不写「它很强大」。
- 描述与他人重复时合并，不重复列条。
- stars 徽章由自动化维护，不在条目内手写。

## PR 自检入口
- 提交前把 [TEMPLATE.md](./TEMPLATE.md) 的「自检清单」过一遍。
- 新仓库/新分类必须同步更新「目录」与顶部 `skills / repos / stars` 徽章数值。
